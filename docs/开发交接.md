# 开发交接

更新时间：2026-07-18
当前分支：`main`
当前阶段：隔离环境管理员与创作者完整流程已通过，等待生产账号人工验收并推进运营功能

本文只维护当前真实状态。历史变更见根目录 [CHANGELOG.md](../CHANGELOG.md)，完整产品规则见
[婚礼作品展示与云相册平台方案](婚礼作品展示与云相册平台方案.md)。

## 接手顺序

1. 阅读根目录 `AGENTS.md`、本文和 `CHANGELOG.md`。
2. 执行 `git status --short`、`git log -5 --oneline`，确认分支及未提交改动。
3. 阅读本次要修改模块的代码、测试和接口文档，不根据交接摘要猜测实现。
4. 修改完成后执行对应最低验证，并同步更新本文与 `CHANGELOG.md`。
5. 只有在代码、测试、文档和必要的部署验证均完成后，才将任务标记为完成。

## 工程与运行环境

| 模块 | 技术与用途 |
| --- | --- |
| `wedding-web` | Vue 3 + Vite，公开官网与后续客户中心 |
| `wedding-console` | Vue 3 + Vite + Element Plus，管理员与创作者工作台 |
| `wedding-server` | Java 17 + Spring Boot + Gradle + Spring Data JPA |
| `deploy` | Nginx、systemd、构建、远程与本机生产发布脚本 |
| `docs` | 产品方案、接口说明与当前交接状态 |

本地默认地址：

- 官网：`http://localhost:5173`
- 工作台：`http://localhost:5174`
- API：`http://localhost:8080`

## 已完成

### 平台基础

- 手机号和密码登录、JWT 无状态认证。
- 用户、系统角色、权限资源 RBAC 模型。
- 管理员开通、启停、重置创作者账号。
- 一个创作者分配多个职业角色。
- 创作者首次改密、头像上传和必填资料完善。
- 前端路由守卫和后端 `AccountSetupFilter` 双重约束首次登录流程。
- 操作日志和 Flyway `V1` 至 `V3`。
- 账号完整流程集成测试。

### 阶段二项目、作品集与图片

- Flyway `V4` 已建立婚礼项目、项目参与者、分类、标签、作品集及关联表。
- 新增项目、分类、标签和作品集 JPA 基础模型，核心业务表统一审计字段、逻辑删除和乐观锁。
- 已实现婚礼项目创建、编辑、详情和分页查询接口。
- 已实现管理员全量分配项目参与者，创作者创建项目后自动成为参与者。
- 已实现管理员全量数据范围和创作者“创建人或参与者”数据范围。
- 项目更新与参与者分配均校验 `version`，已发布项目公共资料保持锁定。
- 项目创建、更新和参与者分配写入操作日志。
- 接口说明见 `docs/阶段二项目接口.md`。
- 已实现管理员分类和标签创建、编辑、启停、排序及重复名称校验。
- 已实现独立或关联项目的作品集创建、编辑、详情和分页查询。
- 已实现作品集分类标签维护、创作者数据范围和管理员共同创作者分配。
- 关联项目作品集只允许项目创建者或参与者共同维护。
- 作品集内容、标签和参与者变更统一校验 `version`，已发布作品集保持锁定。
- 分类、标签和作品集关键操作写入操作日志。
- 接口说明见 `docs/阶段二作品集接口.md`。
- Flyway `V5` 已建立媒体资源和作品图片表，并约束作品集封面图片关系。
- 已实现 JPEG/PNG 真实格式校验、尺寸与 SHA-256 提取、UUID 分层存储。
- 已实现统一水印预览图和缩略图生成，接口不返回原图路径。
- 新生成的预览图使用“糖诗·美学”透明品牌水印，缩略图和原图不加水印。
- JPEG 上传时读取 EXIF Orientation，在缩放前统一归一化旋转和镜像方向；数据库宽高、
  预览图与缩略图均使用转正后的展示尺寸，原图文件保持不变。
- 已实现作品图片批量上传、完整排序、封面设置和逻辑删除接口。
- 图片操作校验作品集参与关系、发布锁和乐观锁，并写入操作日志。
- 上传失败或数据库事务回滚时清理本次未落库文件，逻辑删除后保留物理文件。
- Nginx 配置仅公开 `/media/previews/` 和 `/media/thumbnails/`。
- 接口说明见 `docs/阶段二图片接口.md`。
- 工作台已新增婚礼项目列表、筛选、创建、编辑和管理员参与者分配页面。
- 工作台已新增分类标签创建、编辑、启停和排序页面。
- 工作台已新增作品集筛选、创建、编辑、项目关联、分类标签和共同创作者分配页面。
- 工作台已新增图片批量上传、完整排序、封面设置、预览和逻辑删除页面。
- 工作台路由和导航按 `/content/projects`、`/content/collections`、`/config/content`
  权限资源控制，后端继续执行方法级权限和参与关系校验。

### 阶段三审核发布与官网公开

- Flyway `V6` 已增加作品集提交、审核人、审核时间和驳回原因字段。
- 已实现创作者提交作品集审核，提交前校验图片和有效封面。
- 已实现管理员审核队列、图片批量通过或驳回、作品集资料通过或驳回。
- 已实现删除驳回图片后的状态重算，全部图片通过后进入 `READY`。
- 已实现管理员公开发布、隐藏发布和下架，发布后锁定创作者编辑。
- 待审核期间修改内容会撤回未处理图片并回到草稿，避免审核过期内容。
- 已实现工作台真实运营概览和独立审核中心，移除首页静态运营数据。
- 已实现官网公开分类、作品列表、搜索筛选和作品详情接口。
- 官网首页和详情只展示已发布公开作品及审核通过的水印预览图。
- 接口说明见 `docs/阶段三审核发布与官网接口.md`。

### 阶段三字段审核与修订记录

- Flyway `V7` 已为项目补充审核元数据，并建立统一审核任务、审核项和审核动作记录。
- 项目和作品集首次提交生成全部字段审核项，后续只为变化字段生成新修订。
- 已通过且未修改字段沿用原结论，未修改驳回字段继续阻断整体通过。
- 待审期间编辑会撤回未处理项并恢复上一版有效审核结论。
- 图片审核已同步写入 `review_item`，删除驳回图片会保留 `REMOVED` 修订记录。
- 已实现项目提交、项目审核队列、字段批量审核和项目审核历史接口。
- 已实现管理员发布和下架婚礼项目，发布后锁定公共资料，下架后恢复编辑。
- 作品集已支持关联项目、标题、介绍、分类、标签和封面字段分别审核。
- 工作台项目页可提交和查看审核历史；审核中心可切换项目与作品集并查看修订记录。
- 审核中心默认只展示 `PENDING` 项目和作品集，草稿及其他状态需主动清空或切换审核状态筛选。
- 上述 `V7` 数据库、后端接口和工作台已于 2026-07-17 部署，生产 schema 当前为 `V7`。

### 密码访问、公开项目与逻辑删除

- 项目和作品集发布已支持 `PUBLIC`、`PASSWORD` 和 `HIDDEN`。
- 密码只保存 BCrypt 摘要；密码内容不进入公开列表，通过直接链接输入密码访问。
- 密码正确后建立默认 30 分钟的路径限定 `HttpOnly` Cookie 会话，令牌绑定内容类型、
  内容编号和版本；下架、删除或重新发布后旧会话失效。
- 相同客户端对同一内容的失败访问尝试增加 10 次/10 分钟限制。
- 新增公开婚礼项目分页、关键词与地区筛选、详情、参与创作者和项目下公开作品集接口。
- 官网新增婚礼项目列表、详情和项目/作品集共用密码访问页。
- 项目封面未设置时，公开接口回退到首个已发布公开作品集封面。
- 新增项目和作品集逻辑删除接口；已发布内容必须先下架，项目存在作品集时必须先删除作品集。
- 工作台项目和作品集列表增加删除入口，作品集列表增加已发布内容直接下架入口。
- 管理员可在作品集列表直接发布 `READY` 作品集，并选择公开、密码访问或隐藏，无需再进入
  审核中心详情底部查找发布操作。
- 本批次未新增 Flyway 迁移，继续使用现有 `access_password_hash` 和逻辑删除字段。
- 本批次已完成本地测试、构建、模拟 API 页面检查和生产部署。
- 已使用独立 H2 内存库、临时媒体目录和本地构建产物走通管理员与创作者完整流程：
  创作者首次改密及完善资料、创建项目和作品集、上传图片、设置封面、提交审核，管理员字段与
  图片审核、密码发布、官网错误密码与正确密码访问、下架，以及创作者重新登录后依次删除
  作品集和项目；删除后列表移除且详情接口返回 `404`。

### 生产部署

- SSH 目标：`iot`，公网 IP：`47.104.21.68`。
- 应用目录：`/home/apps/wedding-platform`。
- systemd：`wedding-platform.service`，后端仅监听 `127.0.0.1:8080`。
- MySQL：复用参考项目所在实例，独立 schema 为 `wedding_platform`，独立用户为 `wedding_app`。
- Nginx：`/usr/local/nginx/sbin/nginx`。
- 域名：`https://photo.shop-hz.top/`。
- 路由：`/` 官网、`/console/` 工作台、`/api/` 后端。
- HTTPS 证书覆盖 `photo.shop-hz.top` 和 `www.photo.shop-hz.top`，到期时间为 2026-10-14 23:59:59 UTC。
- 主域名 A 记录已指向 iot；官网、工作台和 API 均已返回成功状态。
- 生产管理员已创建，首次登录要求修改临时密码；引导开关已关闭。
- 正式域名已加入后端 CORS 白名单，浏览器登录验证通过。
- 阶段二 Flyway `V4`、`V5`、后端接口、工作台页面和媒体 Nginx 路由，以及阶段三
  Flyway `V6`、后端接口、工作台页面和官网公开接口，已于 2026-07-17 部署。
- 阶段三 Flyway `V7`、项目与作品集字段审核、统一审核修订记录和工作台分段审核中心
  已于 2026-07-17 部署，生产 schema 当前为 `V7`。
- 审核中心默认待审核队列、婚礼项目发布与下架接口及工作台操作已于 2026-07-17 部署。
- 密码访问、公开婚礼项目、项目与作品集逻辑删除及作品集列表下架已于 2026-07-18 部署。
- 作品集列表直接发布入口已于 2026-07-18 部署。
- 图片 EXIF 方向归一化已于 2026-07-18 部署；新上传 JPEG 会按方向元数据生成正确的预览图
  和缩略图，修复前生成的派生图不会自动重建。
- 审核中心字段布局修复、“糖诗·美学”全站品牌、工作台新登录页和品牌预览水印已于
  2026-07-18 部署；生产 schema 保持 `V7`。
- 最新应用与配置备份目录为 `/home/apps/wedding-platform/backups/20260718-103325`；
  按常规发布约定未执行数据库备份，也没有新增 Flyway 迁移。
- 两个原有项目曾由生产管理员公开发布，随后于 `2026-07-18 07:28` 主动下架；当前均为
  `APPROVED + OFFLINE + PUBLIC`，因此公开婚礼项目列表当前为空。
- 生产公开作品集列表当前返回 1 条作品集。
- `V6` 最终部署备份目录为 `/home/apps/wedding-platform/backups/20260717-230529`。
- `V7` 发布前数据库逻辑备份目录为
  `/home/apps/wedding-platform/backups/20260718-060025-pre-v7`，应用与配置备份目录为
  `/home/apps/wedding-platform/backups/20260718-060026`。
- 上述 `V7` 部署相关的 `20260718` 目录名由生产主机本地时间自动生成，对应部署记录日期为
  2026-07-17。
- 首次部署时 V6 在创建审核队列索引阶段因生产主机 `/tmp` 权限为 `0700` 失败；
  应用文件已由部署脚本自动回滚。确认 V6 的列和外键均已完成后，将 `/tmp` 恢复为
  标准 `1777`、补建索引并修复 Flyway 成功状态，随后重新部署通过。
- 生产主机使用 systemd 219，服务 unit 已使用兼容的 `ReadWriteDirectories` 指令。
- 新增 `deploy/scripts/deploy-local.sh`，供代码仓库位于生产主机时直接完成测试、构建、
  应用备份、发布、回滚和正式域名验收。
- 后续常规发布不执行数据库备份，只备份官网、工作台、后端 JAR、systemd 和 Nginx 配置。
- 数据库只通过 Flyway 向前迁移；应用回滚不会自动回退 schema，新增迁移必须兼容上一版应用。

### “糖诗·美学”品牌升级

- 已建立根品牌资产目录 `brand/`，包含横版组合标、方形标志、深浅背景、单色、favicon 和
  水印版本；`deploy/scripts/sync-brand-assets.sh` 负责同步到官网、工作台和后端资源目录。
- 官网首页、项目列表、项目详情和作品集详情已统一使用“糖诗·美学”品牌，首页 H1、页尾、
  浏览器标题、favicon 和无障碍名称已同步更新。
- 工作台登录页、首次登录页、侧边栏、移动导航、浏览器标题和 favicon 已完成品牌替换，
  用户可见的 `Wedding Archive`、`Wedding Console` 和 `WA` 临时品牌已移除。
- 工作台登录页已使用真实婚礼照片，并补充密码显隐、提交加载和防重复提交，以及
  `401`、`403`、网络和服务端错误的分级提示。
- 新上传图片的预览图使用打包在后端资源中的透明品牌水印；缩略图和原图不加水印。
- 历史预览图本轮没有批量重建，避免在没有独立备份和恢复脚本时覆盖现有派生文件。
- 详细实施结果和验收约束见 `docs/品牌与后台登录页设计要求.md`。

## 当前数据与安全约束

- 生产环境文件：`/etc/wedding-platform/wedding-platform.env`，权限 `0600`，所有者 `root`。
- 数据库密码和 JWT 密钥只保存在服务器环境文件中，不读取、不输出、不提交。
- 证书私钥不提交 Git；远端私钥权限为 `0600`。
- 原图目录不通过 Nginx 公开，后续只公开预览图和缩略图。
- 业务删除采用逻辑删除，图片物理文件长期保留。
- 生产引导必须保持 `BOOTSTRAP_ADMIN_ENABLED=false`。

## 最近验证

2026-07-18 管理员与创作者完整流程已在隔离环境完成验收：

- 使用独立 H2 内存库完成 Flyway `V1` 至 `V7` 迁移，媒体文件写入临时目录，没有连接或修改
  生产数据库和生产媒体目录。
- 创作者首次登录、修改密码、上传头像、完善姓名、职位、服务地区和简介后进入工作台；
  随后创建婚礼项目和关联作品集，上传两张真实 JPEG、设置封面，并分别提交审核。
- 管理员在审核中心通过项目字段、作品集字段和两张图片，分别以密码访问方式发布项目与
  作品集；字段通过后审核网格保持正常两列布局，没有出现内容挤入窄列的问题。
- 官网项目和作品集密码页的错误密码提示正常，正确密码可解锁；作品集两张预览图均成功加载，
  桌面视口没有横向溢出。
- 管理员通过工作台先下架作品集再下架项目；创作者使用修改后的密码重新登录，依次删除作品集
  和项目。两个列表均移除对应记录，两个详情接口均按预期返回 `404`。
- 检查 `1440x1000` 的首次资料完善、项目提交、作品集提交和删除完成截图，页面无控件错位、
  内容遮挡或横向溢出；续跑阶段无浏览器控制台错误和非预期失败请求。
- 本次只进行隔离验收和文档同步，没有修改应用代码、数据库 schema、生产配置或线上状态，
  因此没有执行生产部署。生产账号完整人工流程仍保留为待办。

2026-07-18 审核中心字段布局修复与“糖诗·美学”品牌升级已完成本地验证和生产部署：

- 审核中心字段通过后复选框会移除，但原三列网格仍保留 22 px 第一列，导致字段内容挤入窄列；
  现改为默认两列，仅待审核且显示复选框的字段行使用三列布局。
- 根品牌资产、同步脚本和前端品牌组件已落地；官网与工作台的用户可见临时英文品牌已替换，
  登录页补充真实婚礼照片、密码显隐、加载防重和分级错误提示。
- 后端改为从 `classpath:/brand/watermark.png` 加载透明品牌水印并合成到新预览图右下角；
  缩略图和原图不加水印，历史预览图没有批量重建。
- 执行 `cd wedding-server && JAVA_HOME=/opt/java/jdk17 PATH=/opt/java/jdk17/bin:$PATH
  ./gradlew clean test`，共 19 个测试通过。
- 本机 `/opt/node22` 因系统 glibc 版本不兼容，改用 `node:22.18.0-bookworm-slim` Docker
  分别执行官网和工作台 `npm run build`，两个生产构建均通过；工作台仍提示主 chunk
  超过 500 kB。
- `git diff --check`、`bash -n deploy/scripts/sync-brand-assets.sh` 和
  `bash -n deploy/scripts/build-all.sh` 通过；后端 JAR 已确认包含品牌水印资源。
- 使用构建产物和只读模拟数据检查审核中心字段通过后的布局，桌面和手机端字段内容均使用
  正常内容列，页面没有横向溢出。
- 登录页已检查 `1440x1000`、`1024x768`、`390x844` 和 `360x800`；密码显隐状态实际切换
  正常，官网首页桌面和手机端首屏均显示“糖诗·美学”并露出下一段内容。
- 执行 `./deploy/scripts/deploy-local.sh`，脚本内后端 `clean test` 共 19 个测试通过，官网、
  工作台和 Spring Boot JAR 构建通过；完成应用与配置备份、文件替换、服务重启、Nginx 校验和
  正式域名验收，备份目录为 `/home/apps/wedding-platform/backups/20260718-103325`。
- systemd 服务为 `active`，应用在 16.774 秒内启动，生产 schema 保持 `V7`；官网、工作台
  和公开状态接口返回成功，未认证审核接口返回 `401`，原图测试路径返回 `404`。
- 线上官网和工作台目录与本次构建输出一致，线上后端 JAR SHA-256 为
  `b988258a72650e8aa72cfdc563f5699494db89c6ab542f5d1863599fd9b8786d`，与本地构建一致。
- 正式官网在 `1440x1000`、`390x844` 视口无横向溢出或图片加载失败；正式登录页在相同
  两个视口完整渲染，无横向或纵向溢出，品牌照片和 Logo 均加载成功。

2026-07-18 图片 EXIF 方向修复已完成本地验证和生产部署：

- 根因是原图片依赖 EXIF Orientation 表示拍摄方向，后端 `ImageIO` 读取像素后生成新 JPEG
  时未应用该元数据，导致手机中显示正常的图片在预览图和缩略图中横置、倒置或镜像。
- 后端已在缩放和加水印前归一化 JPEG 的 8 种 EXIF 方向，并将转正后的展示宽高写入
  `media_asset`；PNG 和没有有效方向标记的 JPEG 保持原方向。
- 新增非对称四象限图片回归测试，逐一验证方向 1 至 8 的尺寸和四角像素位置。
- 执行 `cd wedding-server && JAVA_HOME=/opt/java/jdk17 PATH=/opt/java/jdk17/bin:$PATH
  ./gradlew clean test`，共 18 个测试通过。
- 直接使用开发机默认 Node.js 16 执行两个 `npm run build` 时，因缺少
  `node:util.styleText` 失败；随后按仓库既有 Node.js 22 兼容构建方式执行
  `./deploy/scripts/build-all.sh`，官网、工作台和 Spring Boot `bootJar` 构建通过，
  工作台仍提示主 chunk 超过 500 kB。
- `git diff --check` 通过。
- 执行 `./deploy/scripts/deploy-local.sh`，脚本内后端 `clean test` 共 18 个测试通过，官网、
  工作台和 Spring Boot `bootJar` 构建通过；工作台仍提示主 chunk 超过 500 kB。
- 发布脚本完成应用与配置备份、文件替换、服务重启、Nginx 校验和正式域名验收，备份目录为
  `/home/apps/wedding-platform/backups/20260718-085606`；本次没有 Flyway 迁移或数据库备份。
- systemd 服务为 `active`，应用在 17.323 秒内启动；启动日志确认生产 schema 保持 `V7`。
- 官网、工作台、公开状态和公开作品集接口返回 `200`，Nginx 配置检查通过；线上后端 JAR
  与本次构建哈希一致并包含 `metadata-extractor 2.20.0`，官网和工作台产物也与构建输出一致。
- 未使用生产账号创建测试图片，避免增加业务数据。已经生成的错误派生图不会自动重建，
  需要删除并重新上传原图片，或另行执行派生图重建。

2026-07-18 作品集列表直接发布入口生产部署已执行并通过：

- 执行 `./deploy/scripts/deploy-local.sh`，后端 `clean test` 共 17 个测试通过；官网、
  工作台和 Spring Boot `bootJar` 构建通过，工作台仍提示主 chunk 超过 500 kB。
- 发布脚本完成官网、工作台、后端 JAR、systemd 和 Nginx 配置备份、替换、服务重启及
  正式域名验收；备份目录为 `/home/apps/wedding-platform/backups/20260718-083159`。
- 本次没有数据库变更，启动日志确认生产 schema 保持 `V7`，Spring Boot 在 17.032 秒内
  启动完成。
- systemd 服务为 `active`，Nginx 配置检查通过，正式工作台和公开状态接口返回成功。
- 官网、工作台和后端 JAR 线上文件与本次构建产物一致；线上作品集页面 JS 已确认包含
  “发布作品集”入口。
- 未使用生产管理员账号实际发布当前 `READY` 作品集，避免在未确认访问方式前改变线上内容状态。

2026-07-18 密码访问、公开项目、逻辑删除与作品集下架生产部署已执行并通过：

- 执行 `./deploy/scripts/deploy-local.sh`，后端 `clean test` 共 17 个测试通过；官网、
  工作台和 Spring Boot `bootJar` 生产构建通过，工作台仍提示主 chunk 超过 500 kB。
- 发布脚本完成官网、工作台、后端 JAR、systemd 和 Nginx 配置备份、替换、服务重启及
  正式域名验收；备份目录为 `/home/apps/wedding-platform/backups/20260718-081517`。
- 本次没有新增 Flyway 迁移，也未执行数据库备份；启动日志确认生产 schema 保持 `V7`，
  Spring Boot 在 13.067 秒内启动完成。
- systemd 服务为 `active`，Nginx 配置检查通过；官网、婚礼项目页、工作台、审核中心、
  公开状态、分类、作品集和婚礼项目接口均返回 `200`。
- 未认证项目与作品集审核接口返回 `401`，原图测试路径返回 `404`。
- 官网、工作台、后端 JAR、systemd 和 Nginx 线上文件与本次构建及仓库配置一致。
- 公开接口当前返回 0 个婚礼项目和 1 个作品集。访问日志确认两个原有公开项目已在本次部署前
  由管理员主动下架，不是本次发布造成的数据回归。
- 尚未使用生产管理员账号执行浏览器内的新增、审核、密码发布、下架和删除完整人工流程；
  本次部署目的即为后续人工体验并确认需要调整的流程。

2026-07-17 密码访问、公开项目、逻辑删除与作品集下架已执行并通过：

- `cd wedding-server && JAVA_HOME=/opt/java/jdk17 PATH=/opt/java/jdk17/bin:$PATH
  ./gradlew clean test`，共 16 个测试；覆盖密码摘要、短期 Cookie 会话、访问作用域、
  公开项目响应、参与者删除、发布锁、项目作品集删除顺序和逻辑删除后不可见。
- `./deploy/scripts/build-all.sh`，官网、工作台和 Spring Boot `bootJar` 构建通过；
  工作台仍提示主 chunk 超过 500 kB。
- 使用本地构建产物和只读模拟 API，在 `1440x1000` 与 `390x844` 视口检查婚礼项目列表、
  项目详情、密码访问页、项目列表和作品集列表；下架与删除按钮可见，移动端操作区无需
  横向滚动，未发现文字裁切或页面横向溢出。
- `git diff --check` 通过。
- 当时部署未执行；后续已于 2026-07-18 完成生产部署，结果见上方最新验证。

2026-07-17 项目发布与审核中心筛选修复已执行并通过：

- `cd wedding-server && JAVA_HOME=/opt/java/jdk17 PATH=/opt/java/jdk17/bin:$PATH
  ./gradlew clean test`，共 15 个测试；项目审核集成测试覆盖非管理员拒绝、密码可见性拒绝、
  发布锁定、下架原因和下架后恢复编辑。
- `./deploy/scripts/build-all.sh`，官网、工作台和 Spring Boot `bootJar` 构建通过；
  工作台仍提示主 chunk 超过 500 kB。
- `SKIP_TEST=1 SKIP_BUILD=1 ./deploy/scripts/deploy-local.sh`，完成应用与配置备份、发布、
  systemd 重启、Nginx 校验和正式域名验收；备份目录为
  `/home/apps/wedding-platform/backups/20260718-065257`，本次未执行数据库备份。
- systemd 服务为 `active`，Nginx 配置检查通过；官网、工作台、项目页、审核中心、公开状态、
  分类和作品列表接口返回 `200`，未认证项目与作品集审核接口返回 `401`，原图路径返回 `404`。
- MySQL `flyway_schema_history` 最新记录为 `7:1`，启动日志确认 schema 为 `V7` 且无需迁移；
  官网、工作台、后端 JAR、systemd 和 Nginx 线上文件与本次构建输出一致。
- 使用短期认证令牌查询到两个 `APPROVED + READY` 项目；密码可见性发布请求返回
  `400 PASSWORD_VISIBILITY_NOT_SUPPORTED`，请求前后项目状态未变化。
- 使用生产主机 Chromium 在 `1440x1000` 和 `390x844` 视口检查正式项目列表与审核中心：
  两个项目发布按钮均存在且可用，审核中心默认请求包含 `reviewStatus=PENDING`，无控制台错误、
  失败请求、横向溢出或按钮裁切。
- 随后生产管理员通过移动端工作台分别成功公开发布两个项目，审计日志和 Nginx 访问日志均记录
  `POST /api/admin/reviews/projects/{projectId}/publish` 返回 `200`；当前两个项目均为
  `APPROVED + PUBLISHED + PUBLIC`。
- 上述 `20260718` 目录名和日志时间由生产主机本地时间生成，本次部署记录日期为
  2026-07-17。

2026-07-17 `V7` 生产部署已执行并通过：

- 发布前确认生产 MySQL 为 `8.0.32`、Flyway 最新成功版本为 `V6`，`V7` 表和项目审核字段
  均不存在；systemd 服务、Nginx、`/tmp` 的 `1777` 权限和磁盘余量正常。
- 短暂停止后端写入后执行生产数据库逻辑备份并通过 `gzip -t`，备份目录为
  `/home/apps/wedding-platform/backups/20260718-060025-pre-v7`。
- `SKIP_TEST=1 SKIP_BUILD=1 ./deploy/scripts/deploy-local.sh`，完成应用与配置备份、发布、
  systemd 重启、Nginx 校验和正式域名验收；应用备份目录为
  `/home/apps/wedding-platform/backups/20260718-060026`。
- MySQL `flyway_schema_history` 最新版本为 `7` 且执行成功；启动日志确认 `V7` 迁移完成，
  三张审核表、项目四个审核字段和相关索引均存在。
- `https://photo.shop-hz.top/`、`/console/`、`/console/reviews`、公开状态、分类和作品列表接口
  返回 `200`；未认证项目与作品集审核接口返回 `401`，原图测试路径返回 `404`。
- 使用未落盘的 5 分钟短期认证令牌验证当前管理员、项目审核队列、作品集审核队列和运营概览，
  响应结构正确。
- 使用生产主机 Chromium 在 `1440x1000` 和 `390x844` 视口检查正式审核中心的作品集/项目
  切换与详情抽屉，无控制台错误、失败请求、页面横向溢出或按钮文字裁切。

2026-07-17 `V7` 上线前本地验证已执行并通过：

- `cd wedding-server && JAVA_HOME=/opt/java/jdk17 PATH=/opt/java/jdk17/bin:$PATH ./gradlew clean test`，
  共 15 个测试。
- `./deploy/scripts/build-all.sh`，官网、工作台和 Spring Boot `bootJar` 构建通过；
  工作台仍提示主 chunk 超过 500 kB。
- 使用隔离 H2 数据库启动后端，Flyway `V1` 至 `V7` 全部迁移成功，
  `http://127.0.0.1:18080/api/public/status` 返回 `UP`。
- 工作台开发服务通过 `5174` 代理至隔离后端；认证登录、项目与作品集审核队列和
  运营概览接口返回成功。
- 隔离数据首次提交生成 6 个项目字段项、6 个作品集字段项和 1 个图片项；使用
  Chromium 在 `1440x1000` 和 `390x844` 视口检查项目/作品集切换、审核详情、
  批量选择和修订记录，按生产 Nginx 路由约定将 `/media` 替换为本地测试图；
  检查中无控制台错误、失败请求、页面横向溢出或按钮文字裁切。
- `git diff --check`。

2026-07-17 `V6` 生产部署已执行并通过：

- `cd wedding-server && JAVA_HOME=/opt/java/jdk17 PATH=/opt/java/jdk17/bin:$PATH ./gradlew clean test`，
  共 14 个测试。
- `./deploy/scripts/build-all.sh`，官网、工作台和 Spring Boot `bootJar` 构建通过；
  工作台仍提示主 chunk 超过 500 kB。
- `git diff --check`。
- `bash -n deploy/scripts/build-all.sh`。
- `bash -n deploy/scripts/deploy-local.sh`。
- `SKIP_TEST=1 SKIP_BUILD=1 ./deploy/scripts/deploy-local.sh`，完成应用备份、
  发布、systemd 重启、Nginx 校验和正式域名验收；最终备份目录为
  `/home/apps/wedding-platform/backups/20260717-230529`。
- MySQL `flyway_schema_history` 最新版本为 `6` 且执行成功，服务启动日志确认 schema
  当前版本为 `6`；审核队列索引 `idx_work_collection_review_queue` 存在。
- 已比对官网、工作台、后端 JAR、systemd 和 Nginx 配置，线上文件与本次构建输出一致。
- `https://photo.shop-hz.top/`、`/console/`、`/console/reviews` 返回 `200`。
- `https://photo.shop-hz.top/api/public/status`、`/api/public/content/categories`、
  `/api/public/collections` 返回 `200`。
- 未认证 `/api/admin/reviews/collections` 返回 `401`，原图路径
  `/media/originals/deployment-check.jpg` 返回 `404`。

## 下一步

建议依次实现：

1. 使用生产管理员和创作者账号人工走通新增、提交、审核、发布、密码访问、下架和删除流程，
   对照已经通过的隔离流程确认真实账号权限和生产数据范围，并汇总不合理的页面入口与状态流转。
2. 将当前方向错误的图片删除后重新上传，确认新预览图、缩略图和官网展示方向正确，并检查
   新预览图品牌水印。
3. 实现客户评价、咨询线索和首页人工推荐运营配置。
4. 增加官网访问量、作品浏览量和更完整的运营统计。
5. 继续拆分 Element Plus 主 chunk。

## 已知注意事项

- iot 只有 JDK 17，不能将项目编译目标改回 Java 21。
- 当前开发机默认 `java` 为 11，运行 Gradle 时需要显式使用 `/opt/java/jdk17`。
- `www.photo.shop-hz.top` 尚非必需入口；若启用，需要域名所有者额外添加 A 记录。
- 工作台审核发布与官网作品展示已接入真实接口；客户反馈、数据统计和官网运营入口仍未实现。
- 生产审核模型已升级为字段与图片统一修订任务，历史 `V6` 作品集仍可继续查询和提交新修订。
- 密码访问、公开项目、逻辑删除和作品集列表下架已在隔离环境完成完整流程验收，仍待生产账号
  确认真实权限和生产数据范围。
- 密码内容不进入公开列表，需要通过包含内容编号的直接链接访问。
- 项目存在未删除作品集时不能删除；已发布项目和作品集都必须先下架。
- 项目封面上传仍未实现，官网项目封面当前回退使用首个公开作品集封面。
- 图片 EXIF 方向和品牌水印只作用于新生成的派生图，不会自动重建已有预览图和缩略图；
  生产错误图片需删除并重新上传，原物理文件仍按长期保留规则保存。
- 常规部署不备份数据库；涉及破坏性 schema 变更时必须先改为兼容性迁移方案。
- 工作台生产构建仍提示主 chunk 超过 500 kB，后续页面接入时需要继续拆分。
- 线上账号密码属于运行时凭据，不写入交接文档。
- 部署前保留用户未提交改动；不要使用破坏性 Git 命令。

## 常用命令

```bash
# 后端测试
cd wedding-server
./gradlew test

# 全量构建
./deploy/scripts/build-all.sh

# 在当前生产主机直接测试、构建和发布
sudo ./deploy/scripts/deploy-local.sh

# 仅发布应用
SKIP_BUILD=1 ./deploy/scripts/deploy-app.sh

# 证书与 Nginx 输入校验
DRY_RUN=1 ./deploy/scripts/deploy-nginx.sh

# 生产检查
ssh iot 'systemctl is-active wedding-platform.service'
ssh iot '/usr/local/nginx/sbin/nginx -t'
curl https://photo.shop-hz.top/api/public/status
```
