# 开发交接

更新时间：2026-07-17  
当前分支：`main`  
当前阶段：阶段二后端与工作台已上线，准备进入审核发布

本文只维护当前真实状态。历史变更见根目录 [CHANGELOG.md](../CHANGELOG.md)，完整产品规则见
[婚礼作品展示与云相册平台方案](婚礼作品展示与云相册平台方案.md)。

## 接手顺序

1. 阅读根目录 `AGENTS.md`、本文和 `CHANGELOG.md`。
2. 执行 `git status --short`、`git log -5 --oneline`，确认分支及未提交改动。
3. 阅读本次要修改模块的代码、测试和接口文档，不根据交接摘要猜测实现。
4. 修改完成后执行对应最低验证，并同步更新本文与 `CHANGELOG.md`。
5. 只有在代码、测试、文档和必要的部署验证均完成后，才将任务标记为完成。

## 工程与运行环境

| 模块 | 技术与用途 |
| --- | --- |
| `wedding-web` | Vue 3 + Vite，公开官网与后续客户中心 |
| `wedding-console` | Vue 3 + Vite + Element Plus，管理员与创作者工作台 |
| `wedding-server` | Java 17 + Spring Boot + Gradle + Spring Data JPA |
| `deploy` | Nginx、systemd、构建、远程与本机生产发布脚本 |
| `docs` | 产品方案、接口说明与当前交接状态 |

本地默认地址：

- 官网：`http://localhost:5173`
- 工作台：`http://localhost:5174`
- API：`http://localhost:8080`

## 已完成

### 平台基础

- 手机号和密码登录、JWT 无状态认证。
- 用户、系统角色、权限资源 RBAC 模型。
- 管理员开通、启停、重置创作者账号。
- 一个创作者分配多个职业角色。
- 创作者首次改密、头像上传和必填资料完善。
- 前端路由守卫和后端 `AccountSetupFilter` 双重约束首次登录流程。
- 操作日志和 Flyway `V1` 至 `V3`。
- 账号完整流程集成测试。

### 阶段二项目、作品集与图片

- Flyway `V4` 已建立婚礼项目、项目参与者、分类、标签、作品集及关联表。
- 新增项目、分类、标签和作品集 JPA 基础模型，核心业务表统一审计字段、逻辑删除和乐观锁。
- 已实现婚礼项目创建、编辑、详情和分页查询接口。
- 已实现管理员全量分配项目参与者，创作者创建项目后自动成为参与者。
- 已实现管理员全量数据范围和创作者“创建人或参与者”数据范围。
- 项目更新与参与者分配均校验 `version`，已发布项目公共资料保持锁定。
- 项目创建、更新和参与者分配写入操作日志。
- 接口说明见 `docs/阶段二项目接口.md`。
- 已实现管理员分类和标签创建、编辑、启停、排序及重复名称校验。
- 已实现独立或关联项目的作品集创建、编辑、详情和分页查询。
- 已实现作品集分类标签维护、创作者数据范围和管理员共同创作者分配。
- 关联项目作品集只允许项目创建者或参与者共同维护。
- 作品集内容、标签和参与者变更统一校验 `version`，已发布作品集保持锁定。
- 分类、标签和作品集关键操作写入操作日志。
- 接口说明见 `docs/阶段二作品集接口.md`。
- Flyway `V5` 已建立媒体资源和作品图片表，并约束作品集封面图片关系。
- 已实现 JPEG/PNG 真实格式校验、尺寸与 SHA-256 提取、UUID 分层存储。
- 已实现统一水印预览图和缩略图生成，接口不返回原图路径。
- 已实现作品图片批量上传、完整排序、封面设置和逻辑删除接口。
- 图片操作校验作品集参与关系、发布锁和乐观锁，并写入操作日志。
- 上传失败或数据库事务回滚时清理本次未落库文件，逻辑删除后保留物理文件。
- Nginx 配置仅公开 `/media/previews/` 和 `/media/thumbnails/`。
- 接口说明见 `docs/阶段二图片接口.md`。
- 工作台已新增婚礼项目列表、筛选、创建、编辑和管理员参与者分配页面。
- 工作台已新增分类标签创建、编辑、启停和排序页面。
- 工作台已新增作品集筛选、创建、编辑、项目关联、分类标签和共同创作者分配页面。
- 工作台已新增图片批量上传、完整排序、封面设置、预览和逻辑删除页面。
- 工作台路由和导航按 `/content/projects`、`/content/collections`、`/config/content`
  权限资源控制，后端继续执行方法级权限和参与关系校验。

### 生产部署

- SSH 目标：`iot`，公网 IP：`47.104.21.68`。
- 应用目录：`/home/apps/wedding-platform`。
- systemd：`wedding-platform.service`，后端仅监听 `127.0.0.1:8080`。
- MySQL：复用参考项目所在实例，独立 schema 为 `wedding_platform`，独立用户为 `wedding_app`。
- Nginx：`/usr/local/nginx/sbin/nginx`。
- 域名：`https://photo.shop-hz.top/`。
- 路由：`/` 官网、`/console/` 工作台、`/api/` 后端。
- HTTPS 证书覆盖 `photo.shop-hz.top` 和 `www.photo.shop-hz.top`，到期时间为 2026-10-14 23:59:59 UTC。
- 主域名 A 记录已指向 iot；官网、工作台和 API 均已返回成功状态。
- 生产管理员已创建，首次登录要求修改临时密码；引导开关已关闭。
- 正式域名已加入后端 CORS 白名单，浏览器登录验证通过。
- 阶段二 Flyway `V4`、`V5`、后端接口、工作台页面和媒体 Nginx 路由已于
  2026-07-17 部署，生产 schema 当前为 `V5`。
- 新增 `deploy/scripts/deploy-local.sh`，供代码仓库位于生产主机时直接完成测试、构建、
  应用备份、发布、回滚和正式域名验收。

## 当前数据与安全约束

- 生产环境文件：`/etc/wedding-platform/wedding-platform.env`，权限 `0600`，所有者 `root`。
- 数据库密码和 JWT 密钥只保存在服务器环境文件中，不读取、不输出、不提交。
- 证书私钥不提交 Git；远端私钥权限为 `0600`。
- 原图目录不通过 Nginx 公开，后续只公开预览图和缩略图。
- 业务删除采用逻辑删除，图片物理文件长期保留。
- 生产引导必须保持 `BOOTSTRAP_ADMIN_ENABLED=false`。

## 最近验证

2026-07-17 已执行并通过：

- `cd wedding-server && JAVA_HOME=/opt/java/jdk17 PATH=/opt/java/jdk17/bin:$PATH ./gradlew clean test`，共 13 个测试。
- `./deploy/scripts/build-all.sh`，官网、工作台和 Spring Boot `bootJar` 构建通过。
- `bash -n deploy/scripts/build-all.sh`。
- `bash -n deploy/scripts/deploy-local.sh`。
- `./deploy/scripts/deploy-local.sh`，完成测试、全量构建、备份、应用发布、systemd
  重启、Nginx 校验和正式域名验收；备份目录为
  `/home/apps/wedding-platform/backups/20260717-214359`。
- MySQL `flyway_schema_history` 最新版本为 `5` 且执行成功，服务启动日志确认 schema
  当前版本为 `5`。
- 已比对官网、工作台、后端 JAR、systemd 和 Nginx 配置，线上文件与本次构建输出一致。
- 使用临时主配置包含 `deploy/nginx/photo.shop-hz.top.conf` 执行 Nginx `-t`。
- `https://photo.shop-hz.top/` 返回 `200`。
- `https://photo.shop-hz.top/console/` 返回 `200`。
- `https://photo.shop-hz.top/console/projects` 返回 `200`。
- `https://photo.shop-hz.top/api/public/status` 返回 `UP`。
- 正式域名 API 预检返回 `200`，未认证项目请求和错误凭据登录均返回 `401`。
- `/media/originals/deployment-check.jpg` 返回 `404`，原图未公开。
- 携带 `Origin: https://photo.shop-hz.top` 的正确登录请求返回 `200`，错误凭据返回 `401`。

## 下一步

建议依次实现：

1. 进入阶段三，实现图片提交审核、批量通过或驳回及替换流程。
2. 实现作品集可发布状态计算、管理员发布下架和公开访问控制。
3. 将工作台首页的静态运营数据替换为真实统计接口。
4. 根据生产数据量继续拆分 Element Plus 主 chunk。

## 已知注意事项

- iot 只有 JDK 17，不能将项目编译目标改回 Java 21。
- 当前开发机默认 `java` 为 11，运行 Gradle 时需要显式使用 `/opt/java/jdk17`。
- `www.photo.shop-hz.top` 尚非必需入口；若启用，需要域名所有者额外添加 A 记录。
- 工作台项目、作品集、分类标签和图片页面已接入真实接口，但首页运营指标和审核队列仍为静态展示。
- 工作台生产构建仍提示主 chunk 超过 500 kB，后续页面接入时需要继续拆分。
- 线上账号密码属于运行时凭据，不写入交接文档。
- 部署前保留用户未提交改动；不要使用破坏性 Git 命令。

## 常用命令

```bash
# 后端测试
cd wedding-server
./gradlew test

# 全量构建
./deploy/scripts/build-all.sh

# 在当前生产主机直接测试、构建和发布
sudo ./deploy/scripts/deploy-local.sh

# 仅发布应用
SKIP_BUILD=1 ./deploy/scripts/deploy-app.sh

# 证书与 Nginx 输入校验
DRY_RUN=1 ./deploy/scripts/deploy-nginx.sh

# 生产检查
ssh iot 'systemctl is-active wedding-platform.service'
ssh iot '/usr/local/nginx/sbin/nginx -t'
curl https://photo.shop-hz.top/api/public/status
```
